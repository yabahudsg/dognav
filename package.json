{
  "name": "dognav",
  "version": "1.0.0",
  "description": "DogNav - 精选网址导航",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "deploy:cf": "node deploy.js",
    "db:seed": "npx wrangler d1 execute dognav --remote --file=./cloudflare/schema.sql && npx wrangler d1 execute dognav --remote --file=./cloudflare/seed.sql",
    "deploy": "node deploy.js",
    "db:init": "npx wrangler d1 execute dognav --remote --file=./cloudflare/schema.sql && npx wrangler d1 execute dognav --remote --file=./cloudflare/seed.sql"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sql.js": "^1.10.3",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "hono": "^4.4.0"
  }
}
